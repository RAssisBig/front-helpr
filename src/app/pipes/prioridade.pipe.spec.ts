import { PrioridadePipe } from './prioridade.pipe';

describe('PrioridadePipe', () => {
  it('create an instance', () => {
    const pipe = new PrioridadePipe();
    expect(pipe).toBeTruthy();
  });
});
